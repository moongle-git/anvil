{
  "project": "anvil",
  "phase": "6-sqlite-store",
  "steps": [
    {
      "step": 0,
      "name": "design-docs",
      "status": "completed",
      "started_at": "2026-07-12T17:34:03+0900",
      "summary": "ADR-014(SQLite 전환, ADR-002 폐기·ADR-004 유지, 3테이블 DDL·PRAGMA·UPDATE-only saveRun)와 ADR-015(삭제=CASCADE·running 불가, 재실행=포크)를 추가하고 ADR-002에 폐기 상태·ADR-007에 updated_at(15분) 정정을 달았다. ARCHITECTURE는 runs/ 파일 블록을 DB 스키마+artifacts.kind 매핑표로 교체하고 데이터 흐름·상태 관리·요청당 커넥션 규칙을 갱신, PRD는 Phase 6 섹션을 신설하고 상태 파생 규칙을 runs.updated_at으로 정정, UI_GUIDE에 삭제(무채색 진입+인라인 Danger 확인)·재실행(Secondary, resume과 구분)·계보 표시 규격을 추가했다. 코드 무변경.",
      "completed_at": "2026-07-12T17:40:30+0900"
    },
    {
      "step": 1,
      "name": "db-schema",
      "status": "completed",
      "started_at": "2026-07-12T17:40:30+0900",
      "summary": "src/lib/db.ts 신규: openDb(dbPath: string): DatabaseSync — 상위 디렉토리 mkdir -p(:memory: 제외), PRAGMA 3종(journal_mode=WAL·busy_timeout=5000·foreign_keys=ON), DDL을 CREATE TABLE IF NOT EXISTS로 실행(runs·steps·artifacts·schema_version + idx_runs_created_at), schema_version에 1을 최초 1회만 seed. getDefaultDbPath(): string — ANVIL_DB_PATH ?? path.resolve(process.cwd(), 'data', 'anvil.db'). ArtifactKind와 ARTIFACT_KINDS(9개 값: questions|answers|research|context|thesis|criticism|solution|verdict|report)도 src/lib/db.ts에서 export한다 — step 2·4·5는 문자열 리터럴 대신 이것을 import할 것. 마이그레이션 러너 없음(버전 기록만). src/lib/db.test.ts 18개 테스트: 테이블·인덱스·schema_version, PRAGMA 3종 실측, 멱등 재오픈, FK 위반 INSERT 실패, CASCADE 삭제, rerun_of ON DELETE SET NULL, 파일 DB 2커넥션 동시 읽기/쓰기(WAL). package.json(build·test·lint·consult)과 web/package.json(dev·build·start)에 NODE_OPTIONS=--disable-warning=ExperimentalWarning 추가, .gitignore에 /data/ 추가. 새 npm 의존성 0개(node:sqlite 내장). runStore.ts·pipeline·web 소스는 무변경.",
      "completed_at": "2026-07-12T17:44:47+0900"
    },
    {
      "step": 2,
      "name": "run-store-sqlite",
      "status": "completed",
      "started_at": "2026-07-12T17:44:47+0900",
      "summary": "src/lib/runStore.ts를 SQLite 백엔드로 재작성했다. 생성자: new RunStore(dbPath: string) — baseDir가 아니라 DB 파일 경로다(':memory:' 허용). 시그니처 변경: saveReport(runId, markdown): void (구: string 반환 — 파일 경로가 없어졌다). 신규: close(), loadReport(runId): string|null, deleteRun(runId): boolean, createRerun(sourceRunId): RunState, loadRunRecord(runId): {state, updatedAtMs}|null. 신규 named export: RunNotFoundError(runId 필드 보유) — loadRun·createRerun(원본 없음)·saveRun/모든 쓰기(존재하지 않거나 삭제된 run)가 throw한다. STEP_OUTPUT_FILES(파일명 맵)를 STEP_ARTIFACT_KINDS: Record<PipelineStepName, ArtifactKind>로 대체했다(interviewer→questions, context-hunter→context, thesis→thesis, cold-critic→criticism, solution-designer→solution, verdict→verdict; answers·research·report는 step 산출물이 아니라 이 맵에 없다). deriveRunStatus(state, updatedAtMs, nowMs?)는 판정 순서·STALLED_THRESHOLD_MS(15분) 그대로이고 2번째 인자 출처만 state.json mtime → runs.updated_at(epoch ms)으로 바뀌었다. 불변식: saveRun은 UPDATE-only(runs INSERT는 createRun·createRerun만) — 삭제된 run에 대한 좀비 CLI 쓰기는 RunNotFoundError로 실패한다(ADR-014/015). 모든 쓰기가 runs.updated_at을 민다. 읽기는 전부 페일소프트(null) — loadRun만 throw. artifacts.content는 JSON 문자열 통째로(report만 마크다운 원문). createRun/createRerun/saveRun/deleteRun/아티팩트 쓰기는 BEGIN..COMMIT 트랜잭션. createRerun은 idea·interview·questions·answers만 복사하고 research 이후는 복사하지 않으며, 원본에 questions가 있을 때만 interviewer를 completed로 seed한다(없으면 pending). listRuns는 ORDER BY created_at DESC(정렬만 SQL)로 읽고 상태는 deriveRunStatus로 파생하며 RunSummary에 rerunOf?: string가 추가됐다. 테스트: src/lib/runStore.test.ts를 71케이스로 전면 재작성(파일시스템 전제 → tmp DB 파일 + 별도 raw 커넥션으로 손상 주입/검증). npx vitest run src/lib/runStore.test.ts src/lib/db.test.ts = 89/89 통과, npm run lint 통과. ★ 현재 npm run build가 깨져 있다 — src/pipeline/orchestrator.ts:240(saveReport의 void 반환을 reportPath에 대입)이 유일한 컴파일 에러이며, src/pipeline/orchestrator.test.ts·src/pipeline/e2e.test.ts 2개 파일(25케이스)이 구 시그니처(new RunStore(디렉토리), STEP_OUTPUT_FILES, 파일 검증)를 참조해 실패한다. 나머지 30개 테스트 파일 644케이스는 통과한다. 이 깨짐은 step 3(pipeline-cli-wiring)에서 해소한다. web/src/lib/server/runs.ts도 아직 구 전제(getRunsDir·state.json mtime)를 쓰므로 step 5에서 전환한다.",
      "completed_at": "2026-07-12T17:57:56+0900"
    },
    {
      "step": 3,
      "name": "pipeline-cli-wiring",
      "status": "completed",
      "started_at": "2026-07-12T17:53:00+0900",
      "summary": "orchestrator·CLI를 SQLite RunStore에 배선했다. PipelineResult.reportPath?: string → report?: string (마크다운 원문) — 리포트는 더 이상 파일이 아니라 artifacts(kind='report')다. orchestrator는 renderReport 결과를 saveReport(runId, markdown)로 저장하고 원문을 결과에 실어 돌려준다. CLI(src/cli/index.ts): new RunStore(getDefaultDbPath())로 DB를 열고 finally에서 store.close(), 성공 시 리포트 원문을 stdout으로(리다이렉트 가능) 안내 문구는 stderr로 분리해 출력한다. orchestrator.test.ts·e2e.test.ts를 DB 백엔드로 이식(tmp DB 파일 + store API로 검증, 손상 산출물은 스키마 위반 값 저장 또는 별도 커넥션의 raw artifacts 읽기로 대체). web/package.json의 @types/node를 ^20 → ^26.1.0으로 올렸다 — ^20에는 node:sqlite 타입이 없어 next build의 타입체크가 db.ts에서 실패한다. 루트 npm run build 통과, 루트 vitest 32파일 669케이스 전부 통과. ★ 아직 web 서버 레이어 테스트 2파일(34케이스: src/test/server/runs.test.ts, api-routes.test.ts)이 실패한다 — web/src/lib/server/runs.ts가 여전히 getRunsDir()·state.json mtime·report.md 파일 전제이고 fixture가 runs/ 디렉토리다. step 5(web-data-layer)에서 해소한다.",
      "completed_at": "2026-07-12T17:58:00+0900"
    },
    {
      "step": 4,
      "name": "runs-migration",
      "status": "completed",
      "started_at": "2026-07-12T17:57:56+0900",
      "summary": "scripts/migrateRuns.ts 신규: migrateRuns(runsDir, dbPath): MigrationResult {imported[], skipped[], failed[{runId, reason}]}. package.json에 migrate:runs 스크립트 추가(기본 경로 <cwd>/runs + getDefaultDbPath(), CLI 인자로 덮어쓰기 가능). RunStore를 쓰지 않고 openDb로 커넥션을 직접 열어 SQL INSERT한다 — createRun은 새 runId를 만들어 원본 ID(웹 북마크·비교 URL)를 잃고 멱등성이 깨진다. state.json만 RunStateSchema로 검증하고(runs·steps 행을 만들려면 구조를 알아야 한다), 나머지 아티팩트 9종(questions·answers·research·context·thesis·criticism·solution·verdict·report)은 파싱조차 하지 않고 fs.readFileSync 문자열을 artifacts.content에 원문 그대로 넣는다 — 마이그레이션은 검증기가 아니라 이송기다(ADR-014). runs.updated_at은 state.json의 파일 mtime, artifacts.updated_at은 각 원본 파일의 mtime이다(Date.now()를 쓰면 미완료로 죽은 과거 run이 전부 running으로 오판된다). rerun_of는 NULL. 멱등: 이미 있는 run_id는 skipped(덮어쓰지 않는다 — 이송 후 재실행·삭제했을 수 있다). fail-soft: state.json 부재·JSON 파싱 실패·스키마 검증 실패는 failed에 기록하고 나머지 run의 이송을 계속한다. run 하나의 이송은 BEGIN..COMMIT 한 트랜잭션(실패 시 ROLLBACK — 부분 데이터가 남지 않는다). runs/ 디렉토리는 지우지 않고 완료 메시지로 직접 삭제를 안내한다. 외부 API 호출 없음. scripts/migrateRuns.test.ts 15케이스 신규(tmp 픽스처만 사용 — 실제 runs/를 읽지 않는다): 정상 이송·interview 플래그 보존·없는 파일은 아티팩트 미생성·멱등(2회 실행 시 행 수 불변, 이송 후 갱신된 리포트 덮어쓰지 않음, 삭제된 run은 재이송)·손상 격리 3종(state.json 부재/깨진 JSON/스키마 실패 각각 failed + 다른 run 성공, 부분 데이터 잔존 없음)·구 스키마 criticism과 깨진 JSON도 원문 이송 후 loadStepOutput이 null·report.md 원문 왕복·mtime 과거로 되돌리면 deriveRunStatus가 stalled. 실제 이송 실행: 10 runs / 44 steps / 52 artifacts, 실패 0, 2회차는 전부 skipped(행 수 불변). 이송된 DB의 context.json 바이트가 원본 파일과 완전 일치함을 확인했다. 구 run 5개(2026-07-05·07-06 2개·07-07·07-09)는 Phase 2 스키마 드리프트(briefing·marketSizeIndicators·competitorInsight 부재)로 loadStepOutput이 null이지만 이는 이송 실패가 아니라 설계된 읽기 시점 페일소프트다(ADR-011) — 원본 파일을 직접 zod 검증해도 동일하게 실패하며, report(13k~29k자)는 정상 렌더된다. 미완료 run 1개(07-11 cc7df1)는 context-hunter가 error라 아티팩트가 애초에 없다. AC: npx tsc --noEmit·npm run build(web 포함)·npm run lint 전부 통과, npx vitest run 33파일 684케이스 통과, npm run migrate:runs 2회 연속 실행 에러 없음. ★ scripts/는 루트 tsconfig include(['src'])·eslint 대상(src) 밖이라 npm run build/lint가 타입체크하지 않는다 — ad-hoc tsc --noEmit --strict로 별도 검증했다(clean). ★ web 서버 레이어 테스트 2파일(34케이스)은 여전히 실패한다(step 5에서 해소).",
      "completed_at": "2026-07-12T18:03:48+0900"
    },
    {
      "step": 5,
      "name": "web-data-layer",
      "status": "pending"
    },
    {
      "step": 6,
      "name": "delete-run",
      "status": "pending"
    },
    {
      "step": 7,
      "name": "rerun",
      "status": "pending"
    },
    {
      "step": 8,
      "name": "final-verify",
      "status": "pending"
    }
  ],
  "created_at": "2026-07-12T17:34:03+0900"
}