{
  "project": "anvil",
  "phase": "5-source-integrity",
  "steps": [
    {
      "step": 0,
      "name": "design-docs",
      "status": "completed",
      "started_at": "2026-07-12T00:41:00+0900",
      "completed_at": "2026-07-12T00:44:27+0900",
      "summary": "ADR-013(코드 주입 communityVoices·재시도 간 grounding 인용 누적·미검증 URL 링크 박탈·Citation kind 판별자) 추가 및 ARCHITECTURE.md에 research.json 산출물·context-hunter 데이터 흐름·'출처는 사실이다' 패턴 반영 — 코드 무변경"
    },
    {
      "step": 1,
      "name": "grounding-citations",
      "status": "completed",
      "started_at": "2026-07-12T00:44:27+0900",
      "completed_at": "2026-07-12T00:53:34+0900",
      "summary": "재시도 전 시도의 grounding 인용을 누적한다 (ADR-013). 시그니처 — GeminiService.generateValidated는 { data, response } 대신 { data, responses: GenerateContentResponse[] }(형식·검증 실패 시도 포함 전부 누적)를 반환하고, extractCitations(responses: readonly GenerateContentResponse[]): GroundingCitation[]는 배열을 받아 uri 기준 dedupe한다. generateGrounded의 공개 시그니처 { data, citations, webSearchQueries }는 불변이라 contextHunter 호출부는 무변경(webSearchQueries도 전 시도 누적·dedupe). CitationSchema/GroundingCitation에 필수 kind 판별자 추가 — groundingChunks[].web.uri는 'redirect', urlContextMetadata의 SUCCESS retrievedUrl은 'origin'이며 같은 uri면 origin이 이긴다(.default 없음; 구 run은 citations: []라 승격 대상 없음). citations가 비어도 throw하지 않는다."
    },
    {
      "step": 2,
      "name": "research-artifact",
      "status": "completed",
      "started_at": "2026-07-12T00:53:34+0900",
      "completed_at": "2026-07-12T01:03:33+0900",
      "summary": "수집 증거를 runs/{id}/research.json으로 영속화하고 소스 커버리지를 코드 주입 필드로 만든다 (ADR-013). 신규 타입(src/types/research.ts) — SourceCoverageSchema { source: ResearchSourceId, status: 'collected'|'unconfigured'|'failed', count: number, error?: string }, ResearchEvidenceSchema { voices: CommunityVoice[], coverage: SourceCoverage[] }. CommunityVoiceSchema/CommunityVoice가 marketContext.ts에서 research.ts로 이동(순환 import 회피)하고 marketContext.ts가 re-export하므로 기존 import 경로는 불변. CollectedEvidence에 coverage: SourceCoverage[] 추가 — collectAll은 RESEARCH_SOURCE_IDS 전체를 순회해 등록되지 않은 소스를 unconfigured(count 0)로 기록하고, collected+count 0(조사했으나 0건)과 구분한다. 여전히 throw하지 않고 failures[]도 유지한다. RunStore에 saveResearchEvidence(runId, evidence): void / loadResearchEvidence(runId): ResearchEvidence | null 추가 (없거나 손상·검증 실패면 null) — research.json은 STEP_OUTPUT_FILES에 없다(answers.json과 같은 비-step 아티팩트). MarketContextObjectSchema에 researchCoverage: z.array(SourceCoverageSchema).default([]) 추가, CODE_INJECTED_CONTEXT_KEYS = ['citations','researchCoverage'], MarketContextDraftSchema에는 없음, toPromptContext는 citations만 덜어내고 researchCoverage는 하류 프롬프트에 남긴다. runContextHunter의 새 반환 시그니처 — Promise<ContextHunterResult> = { context: MarketContext (citations + researchCoverage 코드 주입됨), evidence: ResearchEvidence }. orchestrator의 context-hunter executeStep이 evidence를 벗겨 store.saveResearchEvidence(runId, evidence)로 저장하고 context만 반환하므로 context.json은 오염되지 않으며, resume(completed)에서는 run()이 호출되지 않아 research.json을 재생성하지 않는다. 프롬프트·communityVoices 처리는 무변경(step 3의 scope)."
    },
    {
      "step": 3,
      "name": "voice-reference-injection",
      "status": "completed",
      "started_at": "2026-07-12T01:03:33+0900",
      "completed_at": "2026-07-12T01:14:02+0900",
      "summary": "communityVoices가 코드 주입 필드가 되었다 (ADR-013) — LLM은 목소리를 ID로만 고르고, 원문·URL·작성자는 코드가 research.json에서 복원한다. 따라서 context.json의 communityVoices[].url은 수집 API가 만든 검증된 URL이며 step 4의 링크 박탈 대상이 아니다(링크를 유지한다). 변경 — MarketContextDraftSchema의 communityVoices가 communityVoiceRefs: z.array(z.string().min(1))로 교체되고, MarketContextObjectSchema는 .omit({communityVoiceRefs:true}).extend({communityVoices: z.array(CommunityVoiceSchema).default([]), ...})로 ref를 산출물에서 덜어낸다(내부 좌표를 남기면 두 개의 진실이 된다). CODE_INJECTED_CONTEXT_KEYS = ['citations','researchCoverage','communityVoices']. promoteLegacyVoices(youtubeVoices 승격)는 무변경으로 계속 동작한다. format.ts에 voiceRefId(index)→'V1' / parseVoiceRef('V1')→0 (형식 오류·V0·선행 0은 null)을 export하고 ID 포맷을 단일 소유하며, voiceBlock에서 URL 출력을 제거했다 — ID는 evidence.voices 인덱스 기준 1-origin 전역 연번(소스별 리셋 없음)이고 0건 소스 '0건' 표기와 실패 섹션은 유지. EVIDENCE_EMPTY_SECTION 문구도 communityVoiceRefs로 갱신. contextHunter의 resolveVoiceRefs가 ID를 evidence.voices로 치환한다 — 범위 밖·형식 오류는 드롭 후 deps.log로 경고(환각 관측 지점), 중복은 하나로 접고, 순서는 LLM 선별 순서를 따르며, 전부 드롭돼 빈 배열이어도 throw하지 않는다. 퍼지 매칭은 하지 않는다. 프롬프트의 출력 JSON 예시에서 목소리 객체가 사라져 LLM이 URL을 타이핑할 자리는 competitors[].url과 sources[]뿐이다(step 4 대상)."
    },
    {
      "step": 4,
      "name": "link-revocation",
      "status": "completed",
      "started_at": "2026-07-12T01:14:02+0900",
      "completed_at": "2026-07-12T01:24:01+0900",
      "summary": "미검증 URL의 링크를 박탈했다 (ADR-013) — 클릭 가능한 링크로 렌더되는 URL은 코드가 API 응답에서 주입한 것뿐이라는 불변식이 렌더러에 섰다. 링크가 남는 것: communityVoices[].url(step 3 이후 코드 주입), citations[].uri 중 kind='origin'(urlContext가 실제로 읽어낸 원본). 링크가 박탈된 것: sources[](LLM 자기보고 — 실측 URL 형식조차 아닌 값이 들어온다), competitors[].url(실측 60% 도달 불가), citations[].uri 중 kind='redirect'(만료되면 404). src/lib/report.ts — inlineCode()로 sources·경쟁사 URL·title/domain 없는 redirect uri를 코드 스팬으로 감싸 마크다운 자동 링크를 차단한다(백틱 포함 값은 더 긴 울타리 + 패딩으로 이스케이프, 표 셀은 tableCell의 파이프 이스케이프를 먼저 통과). '#### 출처'는 '#### 출처 (LLM 자기보고 · 미검증)' + 도입부 경고 인용으로, 경쟁사 표의 '링크' 컬럼은 'URL (미검증)' 텍스트 컬럼으로 바뀌었다. citationLink → citationItem/citationSection으로 갈라져 '#### 검색 인용' 아래 '##### 원본(직접 읽어낸 페이지)'(링크 유지)과 '##### 검색 리다이렉트(만료 가능 · 링크 없음)'(제목 (도메인) — 만료 가능한 검색 리다이렉트) 소분류로 나뉜다. voiceBlock의 벌거벗은 URL은 명시적 [출처](url) 링크가 됐다. web — MarketContextSection의 href={source}가 제거되고(ul[data-source-list] + aria-label '미검증 출처 목록 — 링크 없음' + li title로 스크린리더에도 미검증임을 전달), citations는 CitationList가 kind로 분기해 origin만 <a href>·redirect는 텍스트 + '만료 가능' 무채색 뱃지로 렌더한다(ul[data-citation-list='origin'|'redirect']). CompetitorTable의 '바로가기' <a>는 span[data-unverified-url]+title 텍스트가 됐다. 강등된 요소에 링크색(blue-700)을 쓰지 않는다 — 색이 곧 클릭 가능이라는 신호다(UI_GUIDE 원칙 3). 접기 요약줄도 '출처 N개' → '미검증 출처 N개'(report.ts·web 양쪽). 스키마는 무변경 — sources[]에 z.url()을 걸지도, 필드를 삭제하지도 않았다(형식 검증은 실존을 보장하지 않고, ADR-012의 상보성 논거는 유효하다). 테스트는 클래스가 아니라 role/동작으로 검증한다 — queryAllByRole('link') 부재, origin href 일치, 목소리 링크 회귀 방지. 실 run 육안 검증: 앵커 17개 = 유저 목소리 16(YouTube·HN permalink) + origin 인용 1, sources·경쟁사 URL·redirect 인용 앵커 0개."
    },
    {
      "step": 5,
      "name": "coverage-disclosure",
      "status": "completed",
      "started_at": "2026-07-12T01:24:01+0900",
      "summary": "자료조사 커버리지를 리포트에 드러냈다 (ADR-013) — 수집되지 않은 소스를 침묵으로 숨기지 않는다. src/lib/report.ts: coverageSection()이 시장 맥락 섹션 최상단(briefing·트렌드·경쟁사보다 먼저)에 '### 자료조사 커버리지'를 렌더하고, coverageLine()이 SOURCE_LABELS로 세 상태를 서로 다른 문구로 구분한다 — collected N건 / '0건 (검색됐으나 결과 없음)'(조사했으나 무결과 = 시장 신호) / '미설정으로 수집하지 않음'(키 부재 = 우리 설정 문제) / '수집 실패: {error}'. citations가 0건이면 '웹검색 — 인용 없음 (grounding이 인용을 반환하지 않았다)'를 명시한다(8/8 run에서 조용하던 실패). researchCoverage가 빈 배열이면(구 run) 커버리지 블록을 통째로 생략한다 — 모르는 사실을 지어내지 않는다. voiceBreakdown의 .filter(count > 0)를 제거해 0건 소스가 <summary>에 남고(YouTube 0 · Hacker News 4 · 네이버 0), coverage를 함께 받아 unconfigured는 '미설정'·failed는 '수집 실패'로 0건과 구분한다. web MarketContextSection: ResearchCoverage 카드를 섹션 상단(접힌 영역 밖)에 렌더 — li[data-coverage-source][data-coverage-status]와 p[data-citation-coverage='empty'|'present'] 시맨틱 훅을 노출하고, unconfigured/failed는 severity 팔레트 대신 무채색 Badge(tone='neutral') + font-medium/neutral-900 위계로 눈에 띄게 한다(UI_GUIDE 원칙 3 — 커버리지는 위험도가 아니다). 커버리지 영역에 링크는 0개다. 테스트는 클래스가 아니라 data-* 훅·텍스트·DOM 포함관계로 검증한다(루트 7 + web 7). 실 run 육안 검증: 구 run은 커버리지 블록이 생략된 채 정상 렌더되고, 커버리지를 주입한 run은 세 상태가 각각 다른 문구로 표시된다.",
      "completed_at": "2026-07-12T01:32:49+0900"
    },
    {
      "step": 6,
      "name": "youtube-comment-permalink",
      "status": "pending"
    }
  ],
  "created_at": "2026-07-12T00:41:00+0900"
}