{
  "name": "anvil",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AI 서비스 기획 컨설팅 에이전트",
  "workspaces": [
    "web"
  ],
  "scripts": {
    "build": "NODE_OPTIONS=--disable-warning=ExperimentalWarning tsc && npm run build -w web",
    "test": "NODE_OPTIONS=--disable-warning=ExperimentalWarning vitest run && npm run test -w web",
    "lint": "NODE_OPTIONS=--disable-warning=ExperimentalWarning eslint src && npm run lint -w web",
    "web": "npm run dev -w web",
    "consult": "NODE_OPTIONS=--disable-warning=ExperimentalWarning tsx src/cli/index.ts"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/moongle-git/anvil.git"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "@types/node": "^26.1.0",
    "eslint": "^10.6.0",
    "tsx": "^4.23.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.62.1",
    "vitest": "^4.1.9"
  },
  "dependencies": {
    "@google/genai": "^2.10.0",
    "dotenv": "^17.4.2",
    "zod": "^4.4.3"
  }
}
